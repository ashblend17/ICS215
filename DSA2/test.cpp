#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;
typedef long double ld;
typedef pair<ll, ll> pll;
typedef pair<int, int> pi;
typedef vector<ll> vll;
typedef vector<pll> vpll;
typedef vector<vll> vvll;
typedef vector<int> vi;
typedef vector<vi> vvi;
typedef vector<pi> vpi;
#define pb push_back
#define mp make_pair
#define ff first
#define ss second

void printvi(vi v, int n)
{
    for (int i = 0; i < n; i++)
    {
        cout << v[i] << " ";
    }
    cout << "\n";
}
void printvll(vll v, int n)
{
    for (int i = 0; i < n; i++)
    {
        cout << v[i] << " ";
    }
    cout << "\n";
}
void scanvi(vi &v, int n)
{
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }
}
void scanvll(vll &v, int n)
{
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        
    }
    return 0;
}